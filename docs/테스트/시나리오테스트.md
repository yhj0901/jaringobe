# 시나리오 테스트 결과 — 주간 자동 사이클(루프 완결) · 재판정 반영

> **재판정(2026-09-05, 수정 병합 `341995c` 이후)**: 전용 DB `jaringobe_qa2` 에서 동일 하네스 전량 재실행 + 재판정 하네스 `qa_rejudge.py`(R-01~R-09) 추가. 아래 표의 결과는 재판정 값이며, 1차(2026-09-04) 와 달라진 행은 **[재판정]** 표기. 1차 원문은 `git show 591463c:docs/테스트/시나리오테스트.md`.

> 2026-09-04 / 환경: 로컬 — postgres 16 (`jaringobe-db`, QA 전용 DB `jaringobe_qa`) + uvicorn `:8011`(스케줄러 off, HTTP 계약) + uvicorn `:8012`(스케줄러 on 5초, 실루프) + 가상 시계 하네스(`process_cycle_tick(now)` 직접 호출).
> 외부 경계(네이버 시세·LLM·Expo)는 대역. 소셜 로그인 실키·LLM 실키 부재 → 해당 항목 BLOCKED (테스트계획 5장). 실결제 해당 없음(`simulation=true`).
> 기존 결과(2026-07-09 게스트 홈·소셜 로그인 TS-01~13 전부 PASS)는 git 이력 참조.

## 결과 요약

| 묶음 | 건수 | PASS | FAIL | 비고 |
|------|------|------|------|------|
| M 마이그레이션 백필 (0010→0012) | 6 | 6 | 0 | |
| S1 전체 루프 4사이클 | 40 | 39 | 1 | S1-08b(Low, BUG-006 미처리·DDL 필요) |
| S2 멱등·동시성 | 5 | 5 | 0 | |
| S3 예산 게이트 | 7 | 7 | 0 | **[재판정] S3-07 PASS → BUG-001 수정 확인** |
| S4 미매칭·US·연동·over_budget·자동확정 토글 | 8 | 8 | 0 | |
| S5 배송 미도착·취소 | 9 | 9 | 0 | |
| S6 실패 흐름 | 10 | 10 | 0 | **[재판정] S6-01/02/04 PASS → BUG-002 수정 확인** |
| S7 스킵·휴면·상한·만료 | 9 | 9 | 0 | |
| S8 상태 머신·엣지 | 15 | 15 | 0 | [재판정] S8-03/07/08/09/15 PASS(BUG-003/004/005 수정 확인), S8-04 는 하네스 기대값 수정 |
| R 재판정 추가(qa_rejudge.py) | 21 | 20 | 1 | R-04b → **BUG-012 (Low, 신규 관찰)** |
| H HTTP 계약·보안 | 53 | 52 | 1 | H-20 → BUG-007(미처리). [재판정] H-31/48 PASS |
| LIVE 실 스케줄러 루프 | 2 | 1 | 1 | LIVE-02 → BUG-011(미처리) |
| i18n·UI 정합 | 6 | 5 | 1 | 타임존 변경 UI → BUG-010(미처리) |
| **합계** | **191** | **186** | **5** | FAIL 5건 = 미처리 Low 4(006/007/010/011) + 신규 Low 1(012). **High/Medium 0** |

기준선(자동 회귀, 재판정 재측정): 백엔드 pytest **375 passed / 커버리지 94%**(`jaringobe_qa2_unit`), 프론트 vitest **509 passed / 94.61%**, `tsc --noEmit` 통과. ko/en 키 567/567 일치. 1차 기준선(369/93%)과 비교해 수정 워커가 추가한 6개 테스트만 늘었고 퇴행 없음.

---

## M. 마이그레이션 실데이터 백필 (리비전 0010 → 0011 → 0012)

seed(0010 시점, 사이클 이전 0009 계약): 레거시 사용자 1, confirmed 주문 3건(같은 날 2건 = 중복), `fridge_items.source='order'` 2행 + `manual` 1행.

| TS | 검증 | 결과 | 근거 |
|----|------|------|------|
| M-01 | 기존 confirmed 3건 `inbound_at = confirmed_at`, `delivery_eta = confirmed_at`, `delivery_state='delivered'` 백필 | **PASS** | 3행 모두 `eta_eq=t, inb_eq=t, delivered` |
| M-02 | 새 스캔 ③ 후보(`status='confirmed' AND inbound_at IS NULL AND delivery_eta<=now`) = **0건** → 재고 2배 없음 | **PASS** | `scan3_candidates=0` |
| M-03 | `cycle_start` 백필(`confirmed_at AT TIME ZONE 'Asia/Seoul'`) 후 NOT NULL 승격 | **PASS** | 2026-08-20 / 08-27 |
| M-04 | 같은 사용자·사이클 중복 confirmed → 최신 1건만 유지, 나머지 `cancelled` 강등(삭제 없음) | **PASS** | 08-20 03:00 건 → cancelled, 05:00 건 유지 |
| M-05 | `fridge_items.source` `order`→`delivery` 통합, `manual` 유지; `order_id` 컬럼·부분 인덱스 | **PASS** | delivery 2 / manual 1 |
| M-06 | `users.last_seen_at := updated_at` 백필, `notification_settings` CHECK 에 3종 추가, `alembic heads = 0012` | **PASS** | |

> [재판정] 수정 병합에 `backend/alembic/` 변경 없음(git diff 0건) → M-01~06 결과 승계. `jaringobe_qa2` 에 `upgrade head`(0009→0012) 재실행 성공, `heads=0012` 확인.
> downgrade(0012→0010) 도 빈 DB 에서 왕복 확인. 0011 downgrade 의 비확정 주문 삭제(파괴적)는 문서(db-schema 3-B) 대로.

---

## S1. 전체 루프 — KR/Asia/Seoul, 일요일 배송, 월예산 400,000 KRW 락, kurly 연동, 시세 대역

초기 냉장고: 계란 4 ea(9/9 임박) · 쌀 2000 g · 양파 1 ea(9/10 임박). 지난 사이클(9/6) 식사 완료 1건 → 활성.

| TS | 시점 | 검증 | 결과 |
|----|------|------|------|
| S1-01 | D-5 9/8 09:00 KST | 활성 판정 → 식단 생성 접수 → mock LLM 완료(`ready`, 21끼) | **PASS** |
| S1-02 | | `last_generated_cycle_start=9/13` 접수 즉시 기록, `next_run_at`=D-2 | **PASS** |
| S1-03 | | 생성 프롬프트 힌트에 `Fridge inventory`(쌀 2000 g) + `Use these FIRST`(계란·양파 + expires) + 수량 미감산 RULES | **PASS** |
| S1-04 | | `GET /cycle` stage=generated, `weeklyLimit`=400,000×19/30=253,333.33 (9/1~9/19 누적) | **PASS** |
| S1-05 | D-5 재실행 | 식단 중복 생성 없음(사용자당 사이클 1회) | **PASS** |
| S1-06 | D-2 9/11 | 주문 초안 1건 `draft` | **PASS** |
| S1-07 | | needed = 식단 미완료 끼니 재료 합 − 냉장고 재고 (품목·단위별 정확 일치) | **PASS** |
| S1-08a | | covered = 냉장고가 전량 충당하는 품목만(toBuy=0) | **PASS** |
| S1-08b | | [관찰] 저장 초안 `GET /orders/preview` 의 needed.fromFridge 가 실제 충당분 보존? | **FAIL** — 계란 실제 need 16/재고 4 인데 응답 `needed=12, fromFridge=0, toBuy=12` → BUG-006 |
| S1-09 | | 추정 합계 = 매칭 needed 시세 합(서버 계산) 80,500 | **PASS** |
| S1-10 | | `auto_confirm_at` = 초안 +24h, `simulation=true`, `confirmed_at=null` | **PASS** |
| S1-11 | | 네이버 시세 조회 1회, needed 품목만 | **PASS** |
| S1-12 | | `order_approval` 푸시 시도, 경로 `/orders` | **PASS** |
| S1-13 | | 비-https 상품 링크 → 스냅샷 `link=null` (CWE-79) | **PASS** |
| S1-14 | | `GET /cycle` stage=drafted, `draftOrder`·`currentOrder` 동기 | **PASS** |
| S1-15 | D-2 재실행 | 초안 중복 없음(`uq_orders_open_cycle` + 기존 반환) | **PASS** |
| S1-16 | D-1 9/12 | 24h 무응답 → 게이트 통과 → `confirmed`, `auto_confirmed=true`, `auto_confirm_at=null` | **PASS** |
| S1-17 | | `delivery_eta` = 확정 로컬일 +1일(kurly) 09:00 KST = 9/13 00:00Z | **PASS** |
| S1-18 | | 확정 시 냉장고 즉시 등록 안 함(`inbound_at` null, 배송분 0행) | **PASS** |
| S1-19 | | `GET /cycle` stage=confirmed, `draftOrder=null`, `currentOrder.deliveryEta`(v1.9) | **PASS** |
| S1-20 | D-1 재실행 | 이중 확정 없음 | **PASS** |
| S1-21 | D 9/13 | eta 도달 → needed 15라인만 등록, `source=delivery`, `order_id` FK, `expires_at=null`, `delivered` | **PASS** |
| S1-22 | | `fridge_inbound` 푸시, 경로 `/fridge` | **PASS** |
| S1-23 | | `GET /cycle` stage=delivered, `mealCount=21/completedMealCount=0`(v1.9) | **PASS** |
| S1-24 | D 재실행 | 재고 두 배 되지 않음(`inbound_at` CAS) | **PASS** |
| S1-25 | 식사 완료 3끼 | 냉장고 자동 차감(임박 FIFO), 음수 없음 — 15품목 전후 수량 검산 | **PASS** |
| S1-26 | | 완료 끼니 `fridge_deducted` 스냅샷 | **PASS** |
| S1-27 | | `completedMealCount=3` | **PASS** |
| S1-28 | C2 D-5 9/15 | 지난 사이클 완료 3건 + 접속 → 활성 → 자동 생성 | **PASS** |
| S1-29 | | C2 프롬프트에 **배송 잔여 재고 15품목 전부 되먹임**(냉장고→식단) | **PASS** |
| S1-30 | C2 D-2 9/18 | 잔여 재고만큼 covered 6품목 발생, 추정가 80,500→**41,800** (잔여분만큼 다음 주문 감산) | **PASS** |
| S1-31 | | C1 주문은 delivered 유지 | **PASS** |
| S1-32 | C2 D-1 | 2주차 한도 = 400,000×26/30 − 80,500 = **266,166.67** (0 붕괴 없음) | **PASS** |
| S1-33 | | C2 자동확정 통과 | **PASS** |
| S1-34 | C3 9/27 | 3주차(월 경계) 한도 = 400,000(30일 전액) − C1 − C2 = 277,700 (10월 예산 미차용) | **PASS** |
| S1-35 | | C3 자동확정 통과 | **PASS** |
| S1-36 | C4 10/4 | 4주차(새 달) 한도 = 400,000×10/31 = 129,032.26 | **PASS** |
| S1-37 | | 1~3주차 confirmed 3건 누적 | **PASS** |
| S1-38 | | C4 자동확정 통과 → **4주 연속 한도 붕괴 없음** | **PASS** |

**결론: 루프는 끊기는 지점 없이 한 바퀴(그리고 4바퀴) 돈다.** 이음매별 데이터 흐름(재고→힌트→식단, 식단−재고→초안, 초안→확정→eta→재고, 완료→차감→다음 초안 감산, 확정액→다음 한도)이 전부 실제 DB 값으로 확인됐다.

---

## S2. 멱등·동시성

| TS | 검증 | 결과 |
|----|------|------|
| S2-01 | `mark_inbound` 5세션 동시 실행 → 정확히 1회 True, 배송분 15행(=needed) | **PASS** |
| S2-02 | 사용자 `received:true` ×2 + 스케줄러 inbound 동시 → 재고 1배, `delivered` | **PASS** |
| S2-03 | 같은 초안 동시 승인 3회 → confirmed 1건, 나머지 409 (FOR UPDATE 직렬화) | **PASS** |
| S2-04 | 수동 승인 × 자동확정 스캔 동시(TOCTOU) → confirmed 1건 | **PASS** |
| S2-00 | (사전조건) | **PASS** |

## S3. 예산 게이트

| TS | 검증 | 결과 |
|----|------|------|
| S3-01 | 락 + 한도 초과 → `awaiting_user`/`BUDGET_EXCEEDED`, `auto_confirm_at=null`, `reminded_at` | **PASS** |
| S3-02 | 차단 시 재알림 1회 | **PASS** |
| S3-03 | `GET /cycle` stage=awaiting_user + blockedReason | **PASS** |
| S3-04 | awaiting_user 재판정·재알림 반복 없음 | **PASS** |
| S3-05 | awaiting_user → 사용자 명시 승인 → confirmed(`auto_confirmed=false`) | **PASS** |
| S3-06 | 락 해제 사용자: 초안 322,000 > 한도 253,333 이어도 자동확정 허용 | **PASS** |
| S3-07 | **[결함 탐지]** 초안 80,500 ≤ 한도 253,333.33, D-1 시세 ×4 → 재계산 322,000 > 한도 | **[재판정] PASS** — `awaiting_user/BUDGET_EXCEEDED`, 저장 총액 322,000(재계산 스냅샷), 확정 없음 → BUG-001 수정 확인 |

## S4. 미매칭·US·연동 해제·over_budget·자동확정 토글

| TS | 검증 | 결과 |
|----|------|------|
| S4-01 | 미매칭 비율 > 30% → `UNMATCHED_RATIO` | **PASS** |
| S4-02 | US 사용자: 네이버 미호출, `0.00 USD`, 항상 승인(`US_NO_PRICE`) | **PASS** |
| S4-03 | 연동 해제 → `STORE_DISCONNECTED` (초안 생성은 연동 없이 동작) | **PASS** |
| S4-04 | 연동 해제 상태 승인 → 422 `STORE_NOT_CONNECTED` | **PASS** |
| S4-05 | 식단 over_budget → 초안은 만들되 자동확정 금지(`MEALPLAN_OVER_BUDGET`) | **PASS** |
| S4-06 | `autoConfirm=false` → 열린 초안 `auto_confirm_at=null` | **PASS** |
| S4-07 | off 상태 24h 경과에도 draft 유지 | **PASS** |
| S4-08 | `autoConfirm=true` 복귀 → 지난 시각이면 now+1h | **PASS** |

## S5. 배송 미도착·취소

| TS | 검증 | 결과 |
|----|------|------|
| S5-01 | '아직 안 왔어요' → 등록분 롤백(15→0), eta=응답시각+1일, attempts=1, pending | **PASS** |
| S5-02 | eta 재도달 → 재등록 | **PASS** |
| S5-03 | 3회 '아직' → `delivery_state=unknown` | **PASS** |
| S5-04 | unknown 은 스캔 ③ 제외 | **PASS** |
| S5-05 | 미도착 재고는 냉장고에 없음(다음 초안 과소 발주 방지) | **PASS** |
| S5-06 | unknown → '받았어요' → 등록 + delivered | **PASS** |
| S5-07 | 등록 후 일부 소비 → 취소 → 남은 배송분만 삭제, `inbound_at` 감사 유지 | **PASS** |
| S5-08 | 재취소 → 409 `ORDER_INVALID_STATE` | **PASS** |
| S5-09 | 취소 창(7일) 경과 → 409 `ORDER_CANCEL_WINDOW_CLOSED` | **PASS** |

## S6. 실패 흐름 (기획 5-4 · 5-5)

| TS | 검증 | 결과 |
|----|------|------|
| S6-01 | **[결함 탐지]** 초안 생성 실패(시세 예외, DB 쿼리 이후 raise) 1회 → `stage_attempts=1`, `next_run=+1분` | **[재판정] PASS** — attempts=1, next=00:06Z |
| S6-02 | **[결함 탐지]** 연속 실패 시 1/5/15분 백오프 | **[재판정] PASS** — 6 tick 동안 네이버 호출 **2회**(attempts=2, next=+5분) |
| S6-03 | 실패 중 주문 행 생성 없음 | **PASS** |
| S6-04 | **[결함 탐지]** 3회 초과 실패 → 시세 없이 needed 목록만으로 초안(폴백) | **[재판정] PASS** — 4회차 `force_unmatched` 초안 `draft`/총액 0/`stage=drafted`, 누적 네이버 호출 3회 |
| S6-05 | 시세 복구 후 다음 tick 에 초안 생성(루프 재개) | **PASS** |
| S6-06 | 식단 생성 실패 → plan `failed`, `generate_failed`, 익일 09:00 재시도 예약 | **PASS** |
| S6-07 | 익일 재시도 1회 → 재실패 → 포기, 다음 사이클 D-5 예약, 중복 플랜 없음 | **PASS** |
| S6-08 | 식단 없는 D-2 → 초안 생성 안 함(지난 주 식단 연장 금지) | **PASS** |
| S6-09 | `GET /cycle` stage=generate_failed | **PASS** |
| S6-10 | 완료 0건 → 다음 사이클 `skipped_dormant`(안전장치) | **PASS** |

## S7. 스킵·휴면·일일 상한·초안 만료

| TS | 검증 | 결과 |
|----|------|------|
| S7-01 | 건너뛰기 → 초안 cancelled, stage=skipped_user, skippedCycleStart | **PASS** |
| S7-02 | 스킵 멱등 | **PASS** |
| S7-03 | 스킵 사이클 자동확정 없음 | **PASS** |
| S7-04 | 다음 사이클 정상 생성 | **PASS** |
| S7-05 | 14일 미접속 → skipped_dormant, 생성 없음, `cycle_paused` 1회, `dormant_since` | **PASS** |
| S7-06 | 휴면 2회차 추가 알림 없음 | **PASS** |
| S7-07 | `GET /cycle` stage=skipped_dormant | **PASS** |
| S7-08 | 일일 상한 도달 → 다음 사용자 `deferred_quota`, 익일 09:00 이월(실패 아님) | **PASS** |
| S7-09 | 승인되지 않은 초안 → 다음 D-5 에 `expired` | **PASS** |

## S8. 상태 머신·엣지

| TS | 검증 | 결과 |
|----|------|------|
| S8-01 | confirmed→draft 역행 / inbound 재확정 / failed→* 전이 → 409 | **PASS** |
| S8-02 | D-2 이전 수동 확정(`POST /orders`) 성공 | **PASS** |
| S8-03 | **[결함 탐지]** 확정 주문 있는 사이클에 D-2 스케줄러가 새 초안 생성 안 함 | **[재판정] PASS** — `['confirmed']` |
| S8-04 | **[결함 탐지]** 그 경우 `GET /cycle` stage 가 `drafted` 로 덮이지 않음 | **[재판정] PASS** — `delivered`(eta 9/10 도달 → D-2 tick 스캔 ③ 정상 inbound). **1차 `confirmed` 고정 기대는 하네스 오류**(버그리포트 "하네스 문제"). eta 이전 순수 재현은 R-05b `confirmed` PASS |
| S8-05 | 그 경우에도 이중 확정 없음(부분 유니크) | **PASS** |
| S8-06 | 10/1(목) 사이클 주문이 9/30 확정 | **PASS** |
| S8-07 | **[결함 탐지]** 10/8 사이클 한도가 10/1 사이클 확정액 차감 | **[재판정] PASS** — limit=**100,145.16** = 180,645.16 − 80,500 |
| S8-08 | **[결함 탐지]** 확정 후 타임존 변경 → `next_run_at` 미래 재계산 | **[재판정] PASS** — next=9/15 00:00Z(다음 사이클 D-5) |
| S8-09 | **[결함 탐지]** 타임존 변경 후 같은 사이클 초안 추가 생성 없음 | **[재판정] PASS** — `['confirmed']` |
| S8-10 | awaiting_user 재계산 → 상태·사유·`auto_confirm_at(null)` 유지 | **PASS** |
| S8-11 | 냉장고 전부 충당 → 주문 없음, stage=nothing_to_order, 알림 없음 | **PASS** |
| S8-12 | 푸시 딥링크 외부 URL·스킴·비허용 경로 차단 | **PASS** |
| S8-13 | 푸시 본문 금액·예산·가구 정보 없음 | **PASS** |
| S8-14 | biweekly 창: 9/8 → 9/9(수) 4일 | **PASS** |
| S8-15 | **[결함 탐지]** 자동확정 경합(ALREADY_CONFIRMED) 핸들러가 예외 없이 초안 expired 처리 | **[재판정] PASS** — `expired`, `auto_confirm_at=NULL` |

---

## R. 재판정 추가 시나리오 (`harness/qa_rejudge.py`, `jaringobe_qa2`)

수정된 게이트의 **잔여 경로**와 S8-04 의 **순수 재현**을 위해 추가. 대역·가상 시계는 qa_loop.py 와 공유.

| TS | 검증 | 결과 |
|----|------|------|
| R-01a | [BUG-001] 재계산 322,000 > 한도 → `awaiting_user/BUDGET_EXCEEDED`, `confirmed_at` NULL, `auto_confirmed=false` | **PASS** |
| R-01b | [BUG-001] 차단이 **inbound 이전** — `inbound_at`·`delivery_eta` NULL, 배송분 냉장고 0행 | **PASS** |
| R-01c | [BUG-001] 차단 시 스냅샷이 재계산가로 갱신(322,000), 라인 15/15 유지, `auto_confirm_at` NULL | **PASS** |
| R-01d | [BUG-001] 차단 시 재알림 1회 | **PASS** |
| R-01e | [BUG-001] 차단 후 다음 tick 재판정·네이버 재호출·재알림 없음(멱등) | **PASS** |
| R-01f | [BUG-001] 락 사용자 `confirmed` 0건(어떤 스케줄러 경로로도 초과 확정 없음) | **PASS** |
| R-01g | awaiting_user → 명시 승인(시세 정상) → confirmed(`auto_confirmed=false`), 총액 80,500(승인 시점 재계산) | **PASS** |
| R-02 | [BUG-001 역방향] 초안 322,000 > 한도, D-1 재계산 80,500 ≤ 한도 → 재계산가로 자동확정(초안 스냅샷 미사용) | **PASS** |
| R-03 | [BUG-001] 미매칭 게이트 ④도 재계산 라인 기준 — D-1 40% 미매칭 → `UNMATCHED_RATIO`, 스냅샷에 미매칭 6라인 | **PASS** |
| R-04a | [관찰] D-1 시세 장애 3 tick → `draft` 유지, 잘못된 확정·전이 없음 | **PASS** |
| R-04b | [관찰] D-1 시세 장애 시 재시도가 백오프를 따르는가 | **FAIL** — 3 tick 3회(매 tick 재계산 재시도, `auto_confirm_at` 불변) → **BUG-012 (Low, 회귀 아님)** |
| R-04c | D-1 시세 복구 → 다음 tick 자동확정(루프 지속) | **PASS** |
| R-05a | 사전조건: 9/10 23:00Z 수동 확정 → eta 9/12 00:00Z | **PASS** |
| R-05b | [BUG-003] eta **이전** D-2 스캔 → 초안 추가 없음, `GET /cycle` stage=**confirmed**, `last_stage=drafted` (S8-04 원래 의도의 순수 재현) | **PASS** |
| R-05c | [BUG-003] 확정 사이클에 `order_approval` 알림 없음 | **PASS** |
| R-05d | eta 도달 → inbound → stage=delivered (S8-04 의 `delivered` 가 정상 전이임을 확인) | **PASS** |
| R-06a | [BUG-003] `awaiting_user` 사이클 타임존 변경 → `next_run_at` 미래(9/15) | **PASS** |
| R-06b | [BUG-003] 변경 후 초안 중복 없음, `awaiting_user` 유지 | **PASS** |
| R-07 | [BUG-004 역방향] 9/27 사이클을 9/26 확정 → 10/4 한도 129,032.26(10월 미차감, 월 귀속=cycle_start) | **PASS** |
| R-08 | [BUG-009] `received` StrictBool — `"yes"/"true"/1/0/"1"/None` 전부 422 | **PASS** |
| R-09 | [BUG-008] `backend/app/` 에 `.env NAVER_CLIENT_ID/SECRET` 문구 잔존 0건 | **PASS** |

## H. HTTP 계약·보안 (uvicorn :8011) — 상세는 `보안테스트.md`

| TS | 시나리오 | 결과 |
|----|----------|------|
| H-01~03 | 미인증·위조 토큰 → 401 규격, 스택 미노출 | **PASS** |
| H-04~07 | 타인 주문 approve/cancel/delivery/recalculate → 403 FORBIDDEN, 없는 주문 404, latest/cycle 본인 스코프 | **PASS** |
| H-08~12 | recalculate·settings·approve·skip Origin 불일치 → 403 FORBIDDEN_ORIGIN, GET 은 대상 아님 | **PASS** |
| H-13~14 | `GET /orders/preview?refresh=true` 저장 초안 불변(행·라인·updated_at), 스냅샷 필드 5종 | **PASS** — [재판정] 하네스 psql DB 명 환경변수화 후 **실값 비교**로 재확인(1차는 하드코딩 DB 로 공허 통과 가능성 있었음) |
| H-15~18 | 저장 초안 preview 8회 200 / 즉석 계산 4회째 429 / recalculate 4회째 429 | **PASS** |
| H-19 | `PUT /cycle/settings` 6회째 429 | **PASS** |
| H-20 | [관찰] settings·skip·approve·cancel·delivery 가 5회/분 버킷 공유 | **FAIL → BUG-007** |
| H-21~30 | timezone/anchorWeekday/frequency/extra/items/store/excludeNames/body 검증 422·404 | **PASS** |
| H-31 | [관찰] `received:"yes"` → 422 | **[재판정] PASS** — 422 `VALIDATION_ERROR` (BUG-009 수정 확인) |
| H-32~33 | draft 취소·배송 보정 → 409 | **PASS** |
| H-34 | excludeNames SQLi/XSS 페이로드 무해 | **PASS** |
| H-35~39 | 1탭 승인(서버 재계산·두부 제외·eta 부여·inbound null) → 재승인/재계산 409 → 2번째 `POST /orders` 409 ALREADY_CONFIRMED → skip 409 CYCLE_ALREADY_CONFIRMED | **PASS** |
| H-40~44 | '받았어요' 즉시 등록(delivery 2행) → 재호출 no-op → stage=delivered → 취소 시 롤백 | **PASS** |
| H-45 | `/notifications/settings` 신규 3종 기본 on | **PASS** |
| H-46~47 | 422 응답 스택·경로 미노출, 자격증명 값 없음 | **PASS** |
| H-48 | [관찰] preview notes 에 `.env NAVER_CLIENT_ID/SECRET` 안내 노출 | **[재판정] PASS** — notes=`['PRICE_LOOKUP_UNAVAILABLE']` (BUG-008 수정 확인) |

## LIVE. 실 스케줄러 루프 (uvicorn :8012, `CYCLE_SCHEDULER_ENABLED=true`, 5초)

| TS | 검증 | 결과 |
|----|------|------|
| LIVE-01 | due 사용자 seed → t+5s `generated`(plan ready) → t+10s `drafted`(draft, `auto_confirm_at` 설정) | **PASS** |
| LIVE-02 | [관찰] lifespan 기동 로그 "주간 사이클 스케줄러 시작"·단계 전이 구조화 로그가 프로세스 출력에 나타나는가 | **FAIL(유지)** — 재판정에서도 `app.*` INFO 0줄 → BUG-011 미처리 |

## i18n

| 검증 | 결과 |
|------|------|
| ko/en 평탄화 키 567/567, only-ko 0 · only-en 0, 빈 값 0, 플레이스홀더 불일치 0, en 값에 한글 0 | **PASS** |
| 설계 14-6 요구 키(`cycle.*`, `orders.*`, `fridge.delivery.*`, `notification.*`) 존재, v1.9 문구(`orders.terminal.failed.body` 재계산 언급 없음, `cycle.stage.delivered.body` `{completed}/{total}`) | **PASS** |
| 하드코딩 문자열 스캔(features/cycle·order·fridge·home 비테스트 tsx) — 주석 외 0건 | **PASS** |
| 통화: `formatMoney` Intl.NumberFormat(ko-KR/en-US, currency 코드) — Decimal 문자열 그대로 전달(float 금지) | **PASS** |
| 날짜: `Intl.DateTimeFormat(ko-KR/en-US)` 로 `nextRunAt`·`autoConfirmAt`·`deliveryEta` 로컬 표시 | **PASS** |
| 설정 카드 타임존 **변경** UI(ui-design 14-5 "표시 + 변경") | **FAIL** — 표시만 → BUG-010 |

**총평(재판정): 191건 중 186 PASS / 5 FAIL. 루프는 돈다. 1차 High 2·Medium 2 는 전부 수정 확인. FAIL 5건은 미처리 Low 4건(BUG-006/007/010/011) + 신규 Low 1건(BUG-012, 회귀 아님)으로 GATE 4 를 막지 않는다.**
