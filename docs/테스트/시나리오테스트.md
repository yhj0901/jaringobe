# 시나리오 테스트 결과 (E2E) — 게스트 홈 + 소셜 로그인

> 2026-07-09 / 환경: 로컬 (docker postgres 16 + uvicorn :8000 + next start :3000, 프로덕션 빌드) — 오케스트레이터 주입.
> 자동 주문/실결제 해당 없음 (이번 범위는 결제 미포함).

| TS | 시나리오 | 결과 | 비고 |
|----|----------|------|------|
| TS-01 | 미인증 `GET /users/me` → 401 규격 | **PASS** | detail.code=AUTH_REQUIRED |
| TS-02 | 카카오 authorize → 302 + state 포함 인가 URL | **PASS** | kauth.kakao.com, redirect_uri=백엔드 콜백 |
| TS-03 | `next=https://evil.com` → 인가 URL 오염 없음 | **PASS** | 상대경로 화이트리스트 |
| TS-04 | 애플(P1 미구현) → 404 PROVIDER_NOT_SUPPORTED | **PASS** | |
| TS-05 | 타 Origin POST → 403 FORBIDDEN_ORIGIN | **PASS** | |
| TS-06 | /auth/* 연타 → 429 RATE_LIMITED | **PASS** | IP 버킷 10회/분 (엔드포인트 합산) |
| TS-07 | 변조 state 콜백 → `/login?error=AUTH_INVALID_STATE` 302, 스택 미노출 | **PASS** | |
| TS-08 | `/ko` `/en` 게스트 홈 200 렌더 | **PASS** | SSG 프리렌더 |
| TS-09 | 체험 모드 배지·CTA i18n 노출 | **PASS** | ko "체험 모드"/"예산안 만들어보기", en "Trial mode" |
| TS-10 | `/ko/login?error=AUTH_PROVIDER_DENIED` 배너 조건부 렌더 | **PASS** | 플레인 요청 시 alert 0건 |
| TS-11 | 프론트 rewrites 프록시 → 백엔드 응답 그대로 | **PASS** | :3000/api/v1 → :8000, 동일 오리진 확인 |
| TS-12 | 미인증 `/ko/onboarding` → `/login?next=` 리다이렉트 | **PASS** | 307 |
| TS-13 | 실 provider 소셜 로그인 왕복 (프로덕션) | **PASS** | 2026-07-09 카카오·구글 실계정 로그인 성공 (jaringobe.cloud, users 2·identities 2·세션 생성 확인) |

**결과: 13 PASS / 0 FAIL** (TS-13 은 2026-07-09 OAuth 키 등록 후 프로덕션에서 완료)

브라우저 인터랙션(10초 프롬프트 타이밍, 바텀시트, 예산안 3스텝)은 vitest 컴포넌트 테스트(타이머 mock)로 검증 — 실브라우저 E2E(Playwright)는 스테이징 구축 시 도입 권장.
