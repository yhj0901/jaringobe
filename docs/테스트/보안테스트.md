# 보안 테스트 결과 — 주간 자동 사이클(루프 완결)

> 2026-09-04 / 기준: `docs/설계/security-design.md` 5-5·5-6·5-7(v1.9) + `agents/qa.md` 보안 항목 + 기획 8장 CWE 표. 방법: HTTP(uvicorn :8011, httpx) + 가상 시계 하네스 + 코드 대조. 이전(2026-07-09 S-01~10, 10/10 PASS)은 git 이력 참조.

| # | CWE | 케이스 | 방법 | 결과 |
|---|-----|--------|------|------|
| S-11 | — | 미인증 `GET /cycle`·`/orders/preview`·`/orders/latest`, `POST approve` → 401 `AUTH_REQUIRED` 규격 | H-01/02 | **PASS** |
| S-12 | CWE-287 | 위조 JWT → 401, 스택 미노출 | H-03 | **PASS** |
| S-13 | **CWE-639** | 타인 주문 `approve`/`cancel`/`delivery`/`recalculate` → 403 `FORBIDDEN`; 없는 id → 404; `latest`/`cycle` 본인 행만 | H-04~07 | **PASS** |
| S-14 | CWE-639 | 스케줄러 배치가 `user_id` 스코프 유지 — 스캔 ③ CAS 가 due 행의 `user_id` 로 재스코프, 사용자 단계는 `select(User).where(id==setting.user_id)` | 코드 대조 + S2-01/02 | **PASS** |
| S-15 | **CWE-602** | `POST /orders`·`approve` 에 items/가격/matched 전달 → 422 (`extra='forbid'`); 승인은 서버 재계산(excludeNames 는 이름 필터만) | H-25/28/35 | **PASS** |
| S-16 | **CWE-352** | `POST recalculate`·`PUT settings`·`POST approve`·`POST skip` Origin 불일치 → 403 `FORBIDDEN_ORIGIN`; GET 은 미적용 | H-08~12 | **PASS** — v1.9 결정 이행 확인 |
| S-17 | **CWE-650** | `GET /orders/preview?refresh=true` 가 저장 초안(행·라인·updated_at)을 변경하지 않음, `refresh` 무시 | H-13 | **PASS** — v1.9 결정 이행 확인 |
| S-18 | **CWE-307/770** | preview 즉석 계산 3회/분(4회째 429), 저장 초안 읽기 8회 연속 200, recalculate 3회/분, 액션 5회/분, 429 규격 | H-15~19 | **PASS** |
| S-19 | CWE-770 | [관찰] 액션 리미터가 `cycle/settings`·`skip`·`approve`·`cancel`·`delivery` 5 엔드포인트 **공유 버킷**(합산 5회/분). 스펙은 엔드포인트별 5회/분. IDOR 시도 4회도 본인 버킷을 소진 | H-20 | **주의 → BUG-007 (Low)** |
| S-20 | **CWE-770** | 스케줄러 자기 상한 — 초안 생성 실패 시 백오프(1/5/15분) 대신 **매 tick(60초) 네이버 5페이지×N 재호출**(30 tick 동안 30회) | S6-01/02/04 | **FAIL → BUG-002 (High)** |
| S-21 | **CWE-841** | 상태 머신: confirmed→draft 역행, inbound 주문 재확정, failed→* 전이 → 409; draft 취소/배송 보정 409; confirmed 재승인/재계산 409 | S8-01, H-32/33/36/37 | **PASS** |
| S-22 | **CWE-367** | 수동 승인 × 자동확정 동시, 동시 승인 3회, 두 번째 `POST /orders` → 부분 유니크·FOR UPDATE 로 확정 1건 | S2-03/04, H-38 | **PASS** — 단 경합 핸들러 자체는 BUG-005(Low) |
| S-23 | CWE-20 | `timezone` IANA 화이트리스트, `anchorWeekday` 0~6, `frequency` enum, extra 필드(`cycleStart`) 거부, `excludeNames` ≤40·≤200자, `store` 국가 세트(404), body 누락 422 | H-21~30 | **PASS** |
| S-24 | CWE-20 | [관찰] `received:"yes"` 문자열이 pydantic lax 변환으로 `true` 수용(스펙 422) | H-31 | **주의 → BUG-009 (Low)** |
| S-25 | CWE-89 | `excludeNames` SQLi 페이로드 → 파라미터 바인딩, 테이블 유지 | H-34 | **PASS** |
| S-26 | CWE-79 | 상품 `link` https 만 저장(비-https → null); `title` 은 어댑터에서 태그 스트립 + React 이스케이프; 프론트 `dangerouslySetInnerHTML` 없음(grep) | S1-13, 코드 대조 | **PASS** |
| S-27 | **CWE-601** | 푸시 `data.path` 화이트리스트(`/orders`,`/fridge`,`/mealplan/{id}`) — 외부 URL·커스텀 스킴·`/orders/../admin`·`//evil`·`/settings` 거부 | S8-12 | **PASS** |
| S-28 | **CWE-359** | 푸시 3종 본문에 금액·예산·가구·재료 없음(숫자 0개) | S8-13, sender.TEMPLATES 대조 | **PASS** |
| S-29 | CWE-522 | `user_cycle_settings`·`orders`·`order_items` 에 자격증명 컬럼 없음(스키마 대조); 응답에 키 값 없음 | 스키마, H-47 | **PASS** |
| S-30 | CWE-209/200 | [관찰] `GET /orders/preview` `notes` 에 서버 내부 설정 안내 `".env NAVER_CLIENT_ID/SECRET 필요"` 가 사용자에게 내려감. 값 노출은 아님 | H-48 | **주의 → BUG-008 (Low)** |
| S-31 | CWE-532 | 구조화 로그 `extra` 에 `user_id`·`stage`·`reason`·`cycle_start` 만(금액·재료명 없음) — 코드 대조. 단 로거 미구성으로 실제로는 미출력 | `_log_transition` | **PASS(내용) / 관측성 BUG-011** |
| S-32 | — | 결제 안전: `simulation=true` 고정, `paid/charged` 상태 없음(CHECK 6값), 자동확정 예산 락 게이트 | S1-10, S3-01 | **PASS** |
| S-33 | — | **예산 락 우회**: 게이트 통과 후 서버 재계산 금액이 한도를 초과해도 `locked=true` 자동확정 | S3-07 | **FAIL → BUG-001 (High)** |
| S-34 | — | 미인증 배치 실행 권한: 스케줄러는 서비스 계층에 `user_id` 인자 강제, 전역 세션 없음 | 코드 대조 | **PASS** |
| S-35 | — | 멀티 인스턴스: 컨테이너 `uvicorn` 단일 프로세스(`--workers` 미지정) | `docker-entrypoint.sh` | **PASS(형상)** |

**추가 확인**: 시크릿 하드코딩 없음(`.env` 전용, `.env` 미커밋), `COOKIE_SECURE`·`FRONTEND_ORIGIN` 배포 시 재설정 필요(기존 R-1 유지), 인메모리 리미터(기존 R-2 유지).

**결론: 25건 중 21 PASS, 4 주의(Low), 2 FAIL(High). 취약점으로 분류되는 것은 BUG-001(예산 락 우회, 정직 표시·자동화 신뢰 경계 위반)과 BUG-002(자기 유발 자원 소모·실패 경로 미이행)이다.**
