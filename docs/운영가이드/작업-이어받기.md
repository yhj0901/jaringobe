# 작업 이어받기 (다른 PC에서 계속하기)

> 2026-08-30 기준. 주간 자동 사이클(루프 완결) 작업을 다른 머신에서 이어받기 위한 문서.
> 저장소 상태는 전부 원격에 올라가 있고, 로컬에만 있던 것은 없다.

## 1. 지금 어디까지 왔나

```
기획(GATE 1) ✅ → 설계(GATE 2) ✅ → 구현 ✅ → 마이그레이션(GATE 3) ✅ → QA ⬜ → 문서 ⬜
```

제품의 핵심 루프 — **주간 식단 자동 생성 → 자동 주문 → 배송 시점 냉장고 자동 등록 → 조리·자동 차감 → 잔여분만큼 다음 주문 감산** — 이 코드 상으로 닫혔다. 남은 것은 QA와 문서, 그리고 아래 5장의 미해결 결함이다.

## 2. 브랜치 지도

| 브랜치 | 내용 | 상태 |
|--------|------|------|
| `main` | `efc8376` | 사이클 작업 미반영 |
| **`feature/loop-cycle-base`** | **작업의 중심.** 자동주문 P0 + 알림/웹뷰 + 사이클 구현 + 마이그레이션 0011·0012 | 최신. **여기서 이어받는다** |
| `feature/auto-order-p0-2` | 자동주문 P0 (PR #40) | 리뷰 완료·지적 0건, 머지 대기 |
| `docs/loop-cycle-plan` | 기획서 (GATE 1 통과) | `feature/loop-cycle-base` 에 병합됨 |
| `design/loop-cycle` | 설계 v1.8 (GATE 2 통과) | `feature/loop-cycle-base` 에 병합됨 |
| `feature/app-webview-push` | 앱 웹뷰·푸시 (구버전) | **직접 쓰지 마라.** 리비전 리넘버 후 base 에 통합 완료 |

> 워커 브랜치(`yhj0901/구현-*`, `yhj0901/통합-*`, `yhj0901/인프라-*`)는 로컬 전용이지만
> 그 커밋이 전부 `feature/loop-cycle-base` 에 병합되어 원격에서 도달 가능하다. 따로 받을 필요 없다.

```bash
git clone https://github.com/yhj0901/jaringobe.git && cd jaringobe
git checkout feature/loop-cycle-base
```

## 3. 로컬 환경 세우기

상세는 [install-guide.md](./install-guide.md) / [backend-portable-guide.md](./backend-portable-guide.md).

```bash
cp .env.example .env
# JWT_SECRET 채우기: openssl rand -hex 32
docker compose up -d --build      # db + backend, 마이그레이션 자동 적용
curl http://localhost:8000/health # {"status":"ok","db":true,...}

cd frontend && npm install
echo "BACKEND_URL=http://localhost:8000" > .env.local
npm run dev
```

**포트 주의**: 원래 개발 머신에서는 5432/8000 을 다른 프로젝트가 점유해 `.env` 에
`POSTGRES_PORT=5433` / `BACKEND_PORT=8001` 을 썼다. 새 머신에서 포트가 비어 있으면
`.env.example` 기본값(5432/8000)을 그대로 쓰면 된다. 바꿀 경우 `DATABASE_URL` 과
`TEST_DATABASE_URL` 의 포트, `frontend/.env.local` 의 `BACKEND_URL` 도 같이 맞춘다.

### 테스트
```bash
cd backend && uv run pytest --cov=app     # 360 passed / 커버리지 93%
cd frontend && npx vitest run --coverage  # 503 passed / 94.56%
cd frontend && npm run typecheck
```
Node 20+ 의 실험적 Web Storage 가 jsdom 을 가리는 문제는 `frontend/vitest.setup.ts` 에서
해소해 두었다. 별도 플래그가 필요 없다.

## 4. 확정된 제품 결정 (재논의 금지)

| # | 결정 |
|---|------|
| Q1 | 자동화 = **B→A 단계적**. P1 은 스케줄러 초안 + 알림 + 1탭 승인 + 24h 그레이스 자동확정, P2 에서 전면 자동 |
| Q2 | 사이클 정본 = **주간 롤링**. 월간은 예산 안분 계산기로만 잔존 (`POST /mealplans/monthly` 는 내부/실험용) |
| Q3 | 냉장고 등록 = **`delivery_eta` 시점 자동 등록 + 사용자 보정** |
| Q4 | 실결제(마트 계정 자동화) = **이번 범위 밖**, 로드맵만 |
| Q5 | 브랜치 순서 = auto-order-p0(0009) → app-webview-push(0010 리넘버) → 사이클(0011·0012) |
| Q6 | 식단 생성 트리거 = **조건부 자동**. 활성 사용자만 주 1회 (FR-203 개정) |

근거 문서: `docs/기획/루프완결-주간사이클.md`, `docs/설계/*.md` v1.8

## 5. 코드 리뷰 지적 8건 — 전부 수정 완료 (2026-09-04)

`/code-review` 가 `origin/main..feature/loop-cycle-base` 전체를 리뷰해 나온 8건을 모두 고쳤다.
병합 커밋 `d8e8bff`. **남은 조치 없음.**

### 심각 2건 (직접 재현 검증함)

**① 예산 한도 계산 오류** · `backend/app/domains/budget/service.py`
수정 전에는 `share` 를 이번 사이클(7일)만 안분하면서 `committed` 는 그 달 전체 확정액을 빼,
주문 한 건이 확정되면 2주차부터 한도가 0 이 되어 예산 락 사용자의 자동주문이 남은 달 내내 막혔다.
채택 수식 — 양쪽 기간을 일치시킨다:
```
accrued(month_start, min(cycle_start + cycle_days, next_month))
  − confirmed(month_start, 동일 종료점)
```
월초부터 이번 사이클 끝까지 누적으로 계산하므로 앞선 사이클의 잔액이 이월되고,
`min(..., next_month)` 로 다음 달 예산을 끌어쓰지 않는다.
`docs/설계/architecture.md` 3-9-4 의 수식도 같이 정정했다.

검증 — 월예산 40만원으로 9월 4주 연속 확정 시나리오를 실제 DB 에서 재현:
| 사이클 | 한도 | |
|--------|------|--|
| 1주차 | 160,000 | 144,000 확정 |
| 2주차 | 109,333 | **수정 전이면 여기서 0** |
| 3주차 | 104,267 | |
| 4주차 | 63,760 | 월 경계까지만 안분 — 정상 |

**② `/preview` 레이트리밋 회귀** · `backend/app/domains/order/router.py`
사이클 작업이 `if refresh` 조건을 달아 non-refresh 경로가 무제한이 되어 있었다.
`/preview` 는 저장된 초안이 없으면 네이버 5페이지 + LLM 선택을 부르고 회원 홈 마운트마다 호출된다.
수정 — **refresh 이거나 저장된 초안이 없을 때**(= 전체 빌드가 도는 경우)만 3회/분 제한을 걸고,
저장된 초안을 읽기만 하는 값싼 경로는 통과시킨다.

### 나머지 6건

| # | 위치 | 수정 |
|---|------|------|
| 3 | `order/service.py` | `excludeNames` 제외 후 **잔여 라인 기준으로 총액 재계산** (제외 품목 가격이 총액에 남던 문제) |
| 4 | `cycle/service.py` | 초안 4회 실패를 `generate_failed` + 다음 사이클 시각으로 **커밋해 수렴** (롤백되어 60초마다 무한 재시도하던 문제) |
| 5 | `order/service.py` | 미수령 재확인 ETA 기준을 기존 eta 가 아니라 **응답 시각 +1일** 로 (늦게 답하면 즉시 재-due 되던 문제) |
| 6 | `OrdersController.tsx` | `latest.cycleStart` 와 `preview.cycleStart` 일치 기준으로 분기 (이전 사이클 주문이 이번 사이클 확정 UI 를 막던 문제) |
| 7 | `cycle/service.py` | `MEALPLAN_GENERATING` 충돌 시 **조용히 다음 틱 대기** (매 틱 재시도하던 문제) |
| 8 | `useNotificationSettings.ts` | 저장 중 마지막 변경을 **큐잉 후 재전송** (조용히 유실되던 문제) |

### 검증 수치 (코디네이터 직접 실행)
```
백엔드   367 passed / 커버리지 93% / ruff·mypy 무결점
프론트   505 passed / 94.58% / tsc --noEmit 통과
```

### 설계 문서에 반영해야 할 갭 2건 (미해결)
- `CycleState` 에 배송 완료/전체 수가 없어 프론트가 수치를 못 보여준다 (합의: 일반 진행 문구)
- `failed` 주문 전용 복구 경로가 없다 (합의: `GET /orders/preview?refresh=true` 로 "다시 계산하기")

> `orders.confirmed_at` DROP NOT NULL 누락 갭은 리비전 0011 에서 해소되었으나 db-schema.md 문서에는 미반영.

### 보안 점검 항목 (QA 이관, 미해결)
`GET /orders/preview?refresh=true` 는 GET 인데 초안을 갱신 저장한다. 백엔드 Origin 검증은
`POST/PUT/PATCH/DELETE` 에만 걸려 있어(`main.py` `_STATE_CHANGING_METHODS`) 이 경로는 CSRF
방어를 우회한다. 리미터가 있고 영향이 "자기 초안 재계산"이라 심각도는 낮지만 점검 대상이다.

### 롤백 위험 (변동 없음)
`0011`/`0012` 의 downgrade 는 **파괴적**이다. 비-confirmed 주문과 연관 `order_items` 를 삭제하고,
신규 알림 설정 행 삭제·`delivery` 소스를 `order` 로 되돌린다. 운영 DB 에서 함부로 downgrade 하지 마라.

## 6. 마이그레이션 상태

```
0001 → … → 0008 → 0009(orders) → 0010(notification_app) → 0011(cycle_core) → 0012(cycle_links)
alembic heads: 0012 (단일)
```

**★ 0011 의 최고위험 항목**: 기존 confirmed 주문에 `inbound_at := confirmed_at` 백필.
이게 없으면 이미 냉장고에 들어간 주문을 새 스캐너가 다시 등록해 **재고가 두 배**가 된다.
0011 에 포함되어 있고, 실제 데이터로 재현 검증했다 (백필 후 재등록 스캔 대상 0건).

## 7. 배포 상황 — 백엔드는 로컬에서 돌린다

요금 문제로 상시 백엔드 서버(`api.jaringobe.cloud`)를 내렸다. **백엔드는 로컬 docker 로 돌리는 것이 현재 정본이다.**

### 개발은 전부 로컬로 완결된다 — 터널이 필요 없다

3장대로 하면 백엔드·DB·프론트가 전부 로컬에서 돌고 외부 의존이 없다.

```
브라우저 → localhost:3000 (Next.js) → rewrites 프록시 → localhost:8000 (FastAPI) → localhost:5432 (postgres)
```

검증된 동작: 게스트 홈 200 / API 프록시 401(정상) / 소셜 로그인 authorize 302.
**개발·테스트만 할 거라면 이 장의 나머지는 읽지 않아도 된다.**

### 터널은 "Vercel 프론트를 쓸 때만" 필요하다

`https://jaringobe.cloud` (Vercel) 는 인터넷에 있고 로컬 백엔드는 `localhost` 라 서로 닿지 않는다.
현재 Vercel 프론트는 정적 화면(`/ko`)은 200 이지만 **API 는 전부 502** 다 — `BACKEND_URL` 이
사라진 백엔드를 가리키기 때문이다. 이걸 살리려는 경우에만 터널이 등장한다.

| 상황 | 터널 |
|------|------|
| 내 PC 에서 개발·테스트 | **불필요** |
| 팀원/외부에 시연 | 필요 |
| Vercel 프론트를 살려야 함 | 필요 |

터널이 필요하다면 두 방식이 있다.

1. **임시 (quick tunnel)** — 계정 불필요, 1분.
   ```bash
   cloudflared tunnel --url http://localhost:8000
   ```
   출력된 `https://xxxx.trycloudflare.com` 을 Vercel `BACKEND_URL` 에 넣고 재배포.
   **프로세스가 죽으면 URL 이 바뀌어 Vercel 을 매번 다시 고쳐야 한다** (실제로 두 번 죽었다).

2. **고정 (named tunnel)** — `api.jaringobe.cloud` 고정 주소. 도메인을 이미 보유하고 있어 추가 비용 없음.
   첫 단계가 브라우저 로그인이라 사람이 직접 해야 한다.
   ```bash
   cloudflared tunnel login          # 브라우저가 열림 → Cloudflare 계정 로그인 → 도메인 선택
   cloudflared tunnel create jaringobe-api
   cloudflared tunnel route dns jaringobe-api api.jaringobe.cloud
   cloudflared tunnel run --url http://localhost:8000 jaringobe-api
   ```
   이후 Vercel `BACKEND_URL` 을 `https://api.jaringobe.cloud` 로 **한 번만** 넣으면 끝난다.

터널을 쓸 때는 백엔드 `.env` 를 `FRONTEND_ORIGIN=https://jaringobe.cloud`, `COOKIE_SECURE=true` 로
바꾸고 백엔드를 재기동해야 한다. **로컬 개발로 돌아올 때는 반드시 `http://localhost:3000` / `false`
로 되돌린다** — 안 되돌리면 로컬에서 쿠키가 저장되지 않아 로그인이 되지 않는다.

정식 호스팅처(VPS vs Fly.io·Koyeb + Neon 등)는 **릴리즈 시점에 결정**하기로 미뤄두었다.
이관 절차는 [backend-portable-guide.md](./backend-portable-guide.md) 에 정리되어 있다.

### 소셜 로그인·LLM 키가 비어 있다
`.env` 의 `KAKAO_*`/`GOOGLE_*`/`ANTHROPIC_API_KEY` 가 비어 있다(옛 서버와 함께 사라짐).
- 소셜 로그인 불가 → 로그인이 필요한 기능 전체가 막힌다. 각 콘솔에서 재발급 + 콜백 URL 등록 필요
- `ANTHROPIC_API_KEY` 없으면 AI 식단은 mock 모드

## 8. 다음에 할 일 (권장 순서)

1. ~~5장의 심각 2건 수정~~ ✅ 완료 (2026-09-04, `d8e8bff`)
2. ~~나머지 리뷰 지적 6건~~ ✅ 완료
3. 설계 문서 v1.9 증분으로 갭 2건 반영 (`CycleState` 배송 수치, `failed` 복구 경로) + db-schema 의 `confirmed_at` 표기
4. **PR #40 머지** (리뷰 지적 0건, `mergeable`) → `feature/app-webview-push` 정리 → `feature/loop-cycle-base` PR
5. QA (GATE 4) → 문서

## 9. 오케스트레이션 상태 (참고)

원래 머신에서 Orca 오케스트레이션(`run_017dbfdfe35c`)으로 진행했다. 기획·설계는 Claude Code,
코드 구현은 Codex 가 맡는 역할 분리였다. **이 런타임 상태는 로컬 전용이라 이관되지 않는다.**
다른 머신에서는 새 Run 을 만들거나 일반 작업으로 이어가면 된다. 산출물과 결정은 전부
저장소 문서(`docs/기획/`, `docs/설계/`, `docs/status/`)에 남아 있다.
